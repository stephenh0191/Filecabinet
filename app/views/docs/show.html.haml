%h1= @doc.title
%p= @doc.content
%p
 = link_to "All Docs", docs_path
 = link_to "Fix Doc", edit_doc_path(@doc)
 = link_to "Trash it", doc_path(@doc), method: :delete, data: {confrim: "Are you sure?"}
 